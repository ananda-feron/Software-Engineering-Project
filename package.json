{
  "name": "Software-Engineering-Project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "build:staticsite": "npm run build --prefix staticsite",
    "build": "pnpm i --prefix staticsite && npm run build --prefix staticsite"
  },
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.13",
    "dotenv": "^16.5.0"
  }
}
